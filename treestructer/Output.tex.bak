\documentclass[12pt]{article}
\usepackage{mystyle}
\relpenalty=10000
\binoppenalty=10000
\pagestyle{empty}
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}
\DeclareMathOperator{\Ord}{Ord}
\def\ra{\rightarrow}
\def\Ra{\Rightarrow}
\def\ov{\overline}
\def\ov{\overline}
\begin{document}
{\begin{big} Изначальная формула:

$$  \frac{\ln(x) \cdot x \cdot \sin(x)}{2 \cdot x} $$

\end{big}}

Используем формулу Лагранжа для производной частного двух функций:

 $$  \frac{\ln(x) \cdot x \cdot \sin(x)}{2 \cdot x}  $$

Далее найдём производную от 

$$ \ln(x) \cdot x \cdot \sin(x) $$

Используем формулу Лагранжа для производной произведения:

$$ \ln(x) \cdot x \cdot \sin(x) $$

Далее для производной данного выражения найдём производную от 

$$ \ln(x) \cdot x $$

Используем формулу Лагранжа для производной произведения:

$$ \ln(x) \cdot x $$

Далее для производной данного выражения найдём производную от 

$$ \ln(x) $$

Используем формулу производной логарифмического выражения для функции:

 $$ \ln(x) $$

Далее найдём производную от 

$$x $$

Получаем для данного логарифмического выражения производную: 

$$  \frac{1}{x}  $$

Также найдём производную от 

$$ x $$

Получаем для данного логарифмического выражения производную: 

$$  \frac{1}{x}  \cdot x+\ln(x) \cdot 1 $$

Также найдём производную от 

$$ \sin(x) $$

Используем формулу производной синуса для функции:

 $$ \sin(x) $$

Далее найдём производную от 

$$x $$

Получаем для данного логарифмического выражения производную: 

$$ 1 \cdot \cos(x) $$

Получаем для данного логарифмического выражения производную: 

$$ ( \frac{1}{x}  \cdot x+\ln(x) \cdot 1) \cdot \sin(x)+\ln(x) \cdot x \cdot 1 \cdot \cos(x) $$

Также найдём производную от 

$$ 2 \cdot x $$

Используем формулу Лагранжа для производной произведения:

$$ 2 \cdot x $$

Далее для производной данного выражения найдём производную от 

$$ 2 $$

Также найдём производную от 

$$ x $$

Получаем для данного логарифмического выражения производную: 

$$ 0 \cdot x+2 \cdot 1 $$

Получаем для данного логарифмического выражения производную: 

$$  \frac{(( \frac{1}{x}  \cdot x+\ln(x) \cdot 1) \cdot \sin(x)+\ln(x) \cdot x \cdot 1 \cdot \cos(x)) \cdot 2 \cdot x-\ln(x) \cdot x \cdot \sin(x) \cdot (0 \cdot x+2 \cdot 1)}{(2 \cdot x)^2}  $$



После преобразования функции в итоге получили:

$$  \frac{(( \frac{1}{x}  \cdot x+\ln(x) \cdot 1) \cdot \sin(x)+\ln(x) \cdot x \cdot 1 \cdot \cos(x)) \cdot 2 \cdot x-\ln(x) \cdot x \cdot \sin(x) \cdot (0 \cdot x+2 \cdot 1)}{(2 \cdot x)^2}  $$

 Теперь упростим данное выражение:

Получим финальное выражение:

$$  \frac{(( \frac{1}{x}  \cdot x+\ln(x)) \cdot \sin(x)+\ln(x) \cdot x \cdot \cos(x)) \cdot 2 \cdot x-\ln(x) \cdot x \cdot \sin(x) \cdot 2}{(2 \cdot x)^2}  $$

 \end{document}